apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: base-app-scale-1
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  project: default
  destination:
    name: in-cluster # kubernetes-admin-arena-12
    namespace: staging-scale-1
  source:
    path: base-demo-app
    repoURL: https://github.com/ATGardner/git-source.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
      allowEmpty: false
    syncOptions:
      - PrunePropagationPolicy=foreground
      - Replace=false
      - PruneLast=false
      - Validate=true
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=false
      - ServerSideApply=true
      - RespectIgnoreDifferences=false
