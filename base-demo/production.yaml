apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: base-app-scale-production
  annotations:
    argo.octopus.com/project: "asd"
    argo.octopus.com/environment: "production"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  project: default
  destination:
    name: in-cluster
    namespace: production
  source:
    path: base-demo-app/production
    repoURL: https://github.com/ATGardner/git-source.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - PrunePropagationPolicy=foreground
      - Replace=false
      - PruneLast=false
      - Validate=true
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=false
      - ServerSideApply=true
      - RespectIgnoreDifferences=false
